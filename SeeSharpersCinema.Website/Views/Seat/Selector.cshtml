@model SeeSharpersCinema.Data.Models.ViewModel.SeatViewModel
<a asp-controller="Home" asp-action="Index" type="button" class="btn btn-primary btn-lg">Back</a>
<h1>Stoel selectie</h1>

<h2>@Model.TimeSlot.SlotStart</h2>
    <h2>@Model.TimeSlot.SlotStart</h2>
    <h2>@Model.TimeSlot.Room.Capacity</h2>
<img class="img-fluid" src="@Model.Movie.PosterUrl" />
<ul>
    <li>@Model.Movie.Technique</li>
    <li>@Model.Movie.Duration Min</li>
</ul>

@*<div class="container">
    @for (int i = 1; i <= Convert.ToInt32(ViewData["RoomCapacity"]); i++)
    {
        if (i % 20 == 1) 
        { 
        <div class="row"></div>
        }
        
        if (Model != null)
        {
            if (Model.Contains(i))
            {
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-x-square-fill" viewBox="0 0 16 16" id="@i">
                <path d="M2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2zm3.354 4.646L8 7.293l2.646-2.647a.5.5 0 0 1 .708.708L8.707 8l2.647 2.646a.5.5 0 0 1-.708.708L8 8.707l-2.646 2.647a.5.5 0 0 1-.708-.708L7.293 8 4.646 5.354a.5.5 0 1 1 .708-.708z" />
            </svg>
                
            }
            else
            {
            <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" fill="currentColor" class="bi bi-square" viewBox="0 0 16 16" id="@i">
                <path d="M14 1a1 1 0 0 1 1 1v12a1 1 0 0 1-1 1H2a1 1 0 0 1-1-1V2a1 1 0 0 1 1-1h12zM2 0a2 2 0 0 0-2 2v12a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V2a2 2 0 0 0-2-2H2z" />
            </svg>
                
            }
        }

    }
</div>*@

<a class="btn btn-success" href="@Url.Action("SaveSeatTest", "Seat")">Test Add seats</a>

<a asp-controller="Ticket" asp-action="Options" asp-route-movieId="@Model.Movie.Id" class="btn btn-primary btn-lg">To payment overview</a>
